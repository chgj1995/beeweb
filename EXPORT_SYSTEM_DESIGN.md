# 대용량 데이터 내보내기 기능 구조 기획서 (v9 - 최종본)

## 1. 개요

본 문서는 `Export view`에서 발생하는 대용량 데이터 처리 시 메모리 문제를 해결하기 위한 새로운 내보내기 시스템의 구조를 정의합니다. 기존의 동기식 일괄 처리 방식에서 벗어나, 작업을 분할하여 비동기 병렬 처리하는 방식으로 시스템을 개선하여 안정성과 사용자 경험을 향상시키는 것을 목표로 합니다.

### 1.1. 설계 원칙

-   **간결성:** 코드는 불필요한 복잡성을 피하고 최대한 간결하게 작성합니다. (에러 코드 세분화, 환경 변수 설정 등은 이번 범위에서 제외)
-   **기존 구조 존중:** 기존 코드의 아키텍처와 스타일을 최대한 존중하여 일관성을 유지합니다.
-   **단일 공유 환경:** 모든 사용자가 하나의 시스템을 공유하는 연구개발 환경을 전제로 하며, 사용자별 인증/인가 기능은 구현하지 않습니다.

### 1.2. 핵심 기능

-   **사용자 선택적 데이터 내보내기:** 사용자가 원하는 데이터 종류(센서, 사진 등)를 직접 선택하여 내보낼 수 있습니다.
-   **파일 시스템 기반 상태 관리:** DB 스키마 변경 없이 작업 상태를 관리하여 단순성을 유지합니다.
-   **비동기 처리 및 ZIP 아카이브 출력:** 백그라운드에서 비동기적으로 작업을 수행하고, 결과물은 CSV와 사진 파일을 포함한 `.zip` 아카이브로 제공합니다.
-   **결과 파일 관리:** 스케줄러를 통한 자동 삭제 및 사용자의 수동 삭제 기능을 제공하여 서버 저장 공간을 효율적으로 관리합니다.

---

## 2. 공통부 (Common)

### 2.1. 시간대(Timezone) 처리 원칙
-   **프론트엔드:** Local Time 표시, UTC 전송
-   **API 서버:** UTC 처리
-   **데이터베이스:** UTC 저장

### 2.2. 작업(Job) 정의
-   **저장 위치:** `/tmp/export_jobs/{jobId}.json`
-   **JSON 데이터 구조:** `jobId`, `status`, `progress`, `requestParameters` 등

### 2.3. ZIP 파일 구조
-   `data.csv`와 `pictures/` 폴더로 구성됩니다.

### 2.4. CSV 파일 명세 (신규)
생성될 `data.csv` 파일은 다음의 컬럼과 순서를 따릅니다.
1.  `Time`: UTC, `YYYY-MM-DD HH:MM:SS` 형식
2.  `Hive Name`: Hive의 이름
3.  `Device Name`: 센서 장치의 이름
4.  `Data Type`: 데이터 종류 (예: `temp`, `humi`, `co2`, `weigh`, `in`, `out`)
5.  `Value`: 측정된 값

---

## 3. 데이터베이스부 (DB)

-   **스키마 변경 없음**
-   **파일 저장을 위한 최적화된 조회 함수 (제안):** `streamSensorDataForExport`를 구현하여 메모리 사용량을 최소화합니다.

---

## 4. API 서버부 (tsapi)

### 4.1. 파일 시스템 기반 상태 관리
-   모든 작업 상태는 `/tmp/export_jobs/` 디렉토리 내 `jobId.json` 파일로 관리됩니다.

### 4.2. 신규 API 엔드포인트
-   `POST /api/export`: 신규 작업 생성
-   `GET /api/export/status?jobId={jobId}`: 특정 작업 상태 조회
-   `GET /api/export/download?jobId={jobId}`: 결과 파일 다운로드
-   `DELETE /api/export?jobId={jobId}`: 특정 작업 삭제

-   **`GET /api/export/jobs` (신규)**
    -   **설명:** 현재 시스템에 있는 모든 내보내기 작업의 목록을 조회합니다. (사용자 구분 없음)
    -   **동작:** `/tmp/export_jobs/` 디렉토리의 모든 `.json` 파일을 읽어, 각 작업의 메타데이터 배열을 반환합니다.
    -   **응답:** `[{jobId, status, progress, createdAt, ...}, ...]`

### 4.3. 비동기 작업 및 파일 관리
-   **작업 워커:** `dataTypes`에 따라 선택적으로 데이터를 처리합니다.
-   **파일 자동 정리 스케줄러:** 오래된 작업 파일을 주기적으로 삭제합니다.

---

## 5. 프론트엔드부 (web)

### 5.1. UI/UX 변경 사항
-   **데이터 종류 선택 UI**
-   **작업 이력 테이블 (신규):** 페이지 상단 또는 별도 섹션에 현재 진행 중이거나 완료된 모든 작업 목록을 표시하는 테이블을 추가합니다.
-   **진행 상태 표시 UI**
-   **다운로드 및 삭제 버튼**

### 5.2. 신규 프론트엔드 로직 (`export.js`)
기존 로직은 새로운 비동기 처리 방식으로 **전면 대체**됩니다.

1.  **작업 목록 표시 (페이지 로드 시):**
    -   페이지가 로드되면 `GET /api/export/jobs` API를 호출하여 전체 작업 목록을 가져옵니다.
    -   가져온 목록을 '작업 이력 테이블'에 표시합니다. 테이블의 각 항목은 자신의 상태를 주기적으로 업데이트(폴링)할 수 있습니다.

2.  **작업 생성 (저장 버튼 클릭 시):**
    -   `POST /api/export`를 호출하여 작업을 생성하고, 생성된 작업 정보를 작업 이력 테이블의 맨 위에 추가합니다.

3.  **상태 폴링:**
    -   처리 중(`processing`)인 각 작업 항목은 자신의 상태를 주기적으로 `GET /api/export/status`를 통해 서버에 문의하고 UI를 업데이트합니다.

4.  **파일 다운로드 및 삭제:**
    -   사용자는 작업 이력 테이블 내의 버튼을 통해 특정 작업의 파일을 다운로드하거나 삭제할 수 있습니다.
