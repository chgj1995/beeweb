# Thingspeak 설정파일 추가
mv .env_example .env

## Thingspeak CHANNEL ID 수정
nano .env

# npm install 에러 발생 시 오류 해결
ufw disable

# 종료 및 삭제
sudo docker-compose down -v

# 빌드 및 시작
sudo docker-compose up --build

# 백그라운드에서 시작
sudo docker-compose up -d

# 종료
sudo docker-compose down